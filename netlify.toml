[build]
  command = "npm ci && npm run build"
  publish = "dist"

# Ensure SPA routing works - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
